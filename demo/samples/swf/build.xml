<project name="swf" default="javac" basedir=".">

	<description>Shows how the chart element could be used to render different types of charts.</description>

	<property name="class.name" value="SwfApp" />
	<property name="classes.dir" value="../../../build/classes" />
	<property name="lib.dir" value="../../../lib" />
	<property name="fonts.dir" value="../../fonts" />
	
	<path id="classpath">
        <pathelement location="./build/classes"/>
        <fileset dir="./lib">
            <include name="**/*.jar"/>
        </fileset>
        <pathelement location="${classes.dir}" />
        <pathelement location="${fonts.dir}" />
        <fileset dir="${lib.dir}">
            <include name="**/*.jar"/>
        </fileset>
	    
	</path>

	<target name="javac" description="Compiles the java source files of the test application.">
        <mkdir dir="./build/classes"/> 
        <javac srcdir="./src" destdir="./build/classes" debug="true" optimize="false" deprecation="false">
            <classpath refid="classpath"/>
        </javac>
	    
	</target>

	<target name="compile" description="Compiles the XML report design and produces the .jasper file.">
        <mkdir dir="./build/reports"/> 
        <taskdef name="jrc" classname="net.sf.jasperreports.ant.JRAntCompileTask"> 
            <classpath refid="classpath"/>
        </taskdef>
        <jrc destdir="./build/reports">
            <src>
                <fileset dir="./reports">
                    <include name="**/*.jrxml"/>
                </fileset>
            </src>
            <classpath refid="classpath"/>
        </jrc> 
	</target>

	<target name="fill" description="Fills the compiled report design with data and produces the .jrprint file.">
		<java classname="${class.name}" fork="true">
			<arg value="-Tfill"/>
			<classpath refid="classpath"/>
		</java>
	</target>

	<target name="view" description="Launches the report viewer to preview the report stored in the .jrprint file.">
		<java classname="net.sf.jasperreports.view.JasperViewer" fork="true">
			<arg value="-Fbuild/reports/FontsReport.jrprint"/>
			<classpath refid="classpath"/>
		</java>
	</target>
	
    <target name="swf" description="Generates a SWF version of the report by converting the .jrprint file.">
        <java classname="${class.name}" fork="true" maxmemory="512m">
            <arg value="-Tswf"/>
            <classpath refid="classpath"/>
        </java>
    </target>

    
	<target name="clean" description="Deletes all the generated files.">
		<delete includeemptydirs="true" failonerror="false">
			<fileset dir="./build/classes" includes="**/*.class" />
			<fileset dir="./build/reports" includes="**/*.jasper" />
			<fileset dir="./build/reports" includes="**/*.jasper.jrxml" />
			<fileset dir="./build/reports" includes="**/*.jrprint" />
			<fileset dir="./build/reports" includes="**/*.swf" />
		</delete>
	</target>

</project>
